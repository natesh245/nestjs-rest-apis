import { TodoUpdateRequestDto } from './todo-update-request.dto';

describe('TodoUpdateRequestDto', () => {
  it('should be defined', () => {
    expect(new TodoUpdateRequestDto()).toBeDefined();
  });
});
